export default function RectangularDimensions({length, breadth, height}) {
    return (
        <ul>
            <li>Length: {length} </li>
            <li>Breadth: {breadth}</li>
            <li>Height: {height}</li>
        </ul>
    );
}